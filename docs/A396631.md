# A396631

Publication track for the refined bracelet atlas class `(D, sym)`.

The standalone OEIS program lives in `programs/a396631.py`.

The package module lives in `src/oeis_refined_atlas/a396631.py`.

The shared package combinatorics live in `src/oeis_refined_atlas/common.py`.
