# A396630

Active publication track for the refined bracelet atlas class `(D, ns)`.

The module `src/oeis_refined_atlas/a396630.py` exposes the current terms and a small CLI for regenerating them.

The shared combinatorics live in `src/oeis_refined_atlas/common.py`.